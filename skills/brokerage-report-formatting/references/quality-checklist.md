# Delivery Checklist

Before finishing Agent5, confirm all items:

- Agent4 Markdown remains byte-for-byte unchanged except for inserted chart placeholder lines.
- Agent5 has not added a summary, conclusion patch, evidence index, source appendix, risk disclosure, or disclaimer.
- Every material number and deterministic evidence citation from Agent4 remains intact.
- Every chart placeholder has one unique manifest entry and every required manifest entry is used.
- Every `placement_after` value exactly matches one unique full line in Agent4 Markdown.
- No ASCII chart, “建议插入图表”, raw drawing code, or unsafe chart resource remains.
- Chart titles, units, dates, sources, labels, values, and actual/forecast markers are complete.
- Data gaps remain visible; no missing value has been silently inferred.
